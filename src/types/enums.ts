// eslint-disable-next-line no-shadow
export enum ERoutes {
    LOGIN = '/',
    REGISTER = '/sign-up',
    CHATS = '/messenger',
    SETTINGS = '/settings',
    NOT_FOUND_PAGE = '/400',
    SERVER_ERROR_PAGE = '/500',
}
